# Ignore node_modules (not needed in the final image)
   node_modules

# Ignore local environment files
   .env
   .env.local
   .env.production
   .env.development

# Ignore logs and temporary files
   npm-debug.log
   yarn-error.log
   .cache
   *.swp

# Ignore build output (optional, as it will be recreated in the container)
   dist
   build
   .git